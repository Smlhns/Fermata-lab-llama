# Optional packages for memory-efficient CUDA training.
# Install only in a CUDA environment with matching PyTorch support.
bitsandbytes>=0.43.0
accelerate>=1.3.0

# FlashAttention is hardware/platform sensitive. Install manually if your CUDA setup supports it:
# pip install flash-attn --no-build-isolation
